apiVersion: v2
name: wireguard
description: "Garuda WireGuard pod with optional FRR sidecar in the same network namespace."
type: application
version: 0.3.0
appVersion: "0.1.0"

dependencies:
  - name: frr-sidecar
    version: "0.2.0"
    repository: "oci://ghcr.io/garuda-tunnel/charts"
